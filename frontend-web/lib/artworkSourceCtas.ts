// Shared CTA labels for the three ways to add an artwork to a session.
// Used by both the new-session composer (ExploreSessionView) and the
// ongoing-session action bar (ContextualActionBar) so the surfaces can't drift.
export const ARTWORK_CTA_UPLOAD_PHOTOS = 'Upload photos';
export const ARTWORK_CTA_SCAN_ARTWORK = 'Scan artwork';
export const ARTWORK_CTA_ADD_FROM_COLLECTION = 'Add from my collection';
export const ARTWORK_CTA_ADD_MENU = 'Add artwork';
