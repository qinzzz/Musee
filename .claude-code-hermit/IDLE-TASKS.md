# Idle Tasks
<!-- Tasks your hermit works through during downtime.
     Only active when idle_behavior is "discover" in config.
     Each task runs in its own session, capped by idle_budget.
     Mark items [x] to skip them. Remove when no longer relevant. -->
