// Bumping this version intentionally signs the device out. Rotating secrets
// are validated by the server instead of being migrated between key versions.
export const AUTH_REFRESH_TOKEN_STORAGE_KEY = 'com.musee.auth.refresh-token.v1';
